//for calling later we need to adjust fromm ow .. pre bt hole change kore nebo 
//zustand er parent -- child calling 
import type { ReactNode } from "react";

export const WorkflowProvider = ({ children }: { children: ReactNode }) => {
  return <>{children}</>;
};
//pore themes lagle nie nebo